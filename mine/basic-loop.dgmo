  chart: sequence
  title: Basic Loop                                                                                                                       
  loop retry 3 times
    User -> Server: POST /api/data
    Server -> User: 503 Service Unavailable
  User -> API: GET /users
  if authorized
    loop each page
      API -> DB: SELECT * FROM users LIMIT 50
      DB -> API: rows
    API -> User: 200 OK
  else
    API -> User: 403 Forbidden